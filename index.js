const generateMarkdown = require("./node_modules/util/generateMarkdown");
const inquirer = require("inquirer");
const fs = require("fs");

inquirer.prompt([
    {
        type: "input",
        
    },
])

function init() {

}

init();